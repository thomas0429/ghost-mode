{
  "name": "imperium-webhook-server",
  "version": "1.0.0",
  "description": "Imperium AI Agent Tool Server — handles all Vapi tool calls",
  "main": "webhook-server.js",
  "scripts": {
    "start": "node webhook-server.js",
    "dev": "nodemon webhook-server.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
